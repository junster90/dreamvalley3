<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#miq-navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">
        MIQ
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="miq-navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to(:Home, root_path) %></li>
        <% if current_user %>
          <li><%= link_to(:Goals, user_goals_path(user_id: current_user.id)) %></li>
          <li><%= link_to(:Profile, user_path(current_user)) %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
            <li><%= link_to current_user.first_name || current_user.email, user_path(current_user) %></li>
            <li><%= link_to "Logout", destroy_user_session_path %></li>
        <% else %>
            <li><%= link_to "Login", user_omniauth_authorize_path(:mindvalley) %></li>
        <% end %>
      
      </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>